This project demonstrates how to deploy a web server on AWS and make it accessible through a custom domain name. The goal was to connect networking concepts such as IP addressing, DNS, routing, and web hosting by deploying NGINX on an EC2 instance and configuring DNS records to point a purchased domain to the server.

key commands used:
sudo yum install -y nginx
sudo systemctl enable nginx
sudo systemctl start nginx
