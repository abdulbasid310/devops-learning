In this project I created a multi-container application that consists of a simple Python Flask web application and a Redis database. The flask application uses Redis to store and retrieve data. I used docker compose to manage the multi-container application.


